name: Stale

on:
  schedule: [{ cron: "0 3 * * *" }]
  workflow_dispatch:

permissions:
  issues: write
  pull-requests: write

jobs:
  stale:
    uses: muffy86/infra-automation/.github/workflows/stale.yml@main
